<nav class="navigation left">
  <ul class="links">
    <li class="links item">
      {{#foreach @blog.navigation}}
        <a href="{{url}}" class="link-item" id="{{slug}}-button">{{label}}</a>
      {{/foreach}}
    </li>
  </ul>
</nav>
